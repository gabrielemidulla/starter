# Database Configuration [Docker (PostgreSQL)]
DB_HOST="localhost"
DB_PORT="5432"
DB_NAME="monolith_db"
DB_USER="postgres"
DB_PASSWORD="postgres"

# Server Configuration (Hono + tRPC)
PORT="4000"
NODE_ENV="development"

# Client Configuration (SvelteKit)
VITE_API_URL="http://localhost:4000"